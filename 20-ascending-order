public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    //code here
    /* sol 1
    if(a<=b && b<=c) {
        return true;
    }
    return false; */

    // sol 2
    if(a<=b && b<=c) {
        return true;
    }
    return false;
}