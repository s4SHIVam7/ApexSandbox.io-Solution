public String ageGroup(Integer n) {
    //code here
    Integer age = n;
    if(age >= 22)
        return 'Adult';
    else if(age >= 15 && age < 22)
        return 'Youth';
    else if(age >= 2 && age < 15 )
        return 'Child';
    
    return 'Infant';
}